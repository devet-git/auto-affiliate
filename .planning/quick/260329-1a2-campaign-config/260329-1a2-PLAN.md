# Plan: cấu hình các chiến dịch

## Goal
Implement missing campaign configure capability.
The `Campaigns.tsx` UI Settings button was a dead UI element, and the backend router lacked the `PUT` endpoint to configure it.

## Tasks
- [x] Backend: Add `PUT /campaigns/{campaign_id}` in `server/app/domains/campaign/router.py`.
- [x] Frontend: Implement Settings modal in `web/src/pages/Campaigns.tsx` storing changes to the new endpoint and updating React state.
