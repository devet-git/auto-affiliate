---
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - server/app/domains/shopee_crawler/router.py
autonomous: true
requirements: []
---

# Plan 02-03: Fix Playwright Async Loop Clash

<objective>
Fix the "Playwright Sync API inside the asyncio loop" error in crawler endpoints. FastAPI safely offloads `def` endpoints (synchronous) to an external threadpool, preventing blocking of the main asyncio event loop, which `sync_playwright` currently crashes against.
</objective>

<tasks>

<task>
<description>Change scraper and converter endpoints to synchronous</description>
<read_first>
- server/app/domains/shopee_crawler/router.py
</read_first>
<action>
Sửa trực tiếp file `server/app/domains/shopee_crawler/router.py`:

Chuyển đổi 2 endpoint gọi Playwright sang synchronous context:
1. Đổi `async def trigger_shopee_search(...)` thành `def trigger_shopee_search(...)`.
2. Đổi `async def trigger_affiliate_conversion(...)` thành `def trigger_affiliate_conversion(...)`.

*Lưu ý:* Endpoint `upload_shopee_session` có dùng `await file.read()`, do đó vẫn giữ nguyên `async def` vì nó không gọi Playwright.
</action>
<acceptance_criteria>
`trigger_shopee_search` is declared as `def`
`trigger_affiliate_conversion` is declared as `def`
</acceptance_criteria>
</task>

</tasks>

<verification>
Restart fastapi dev server, call `/api/v1/crawler/shopee/search`. Giờ đây app sẽ pass qua stage validation router, và chạy sync_playwright bình thường ở threadpool.
</verification>

<must_haves>
- Giải quyết triệt để lỗi Playwright Async vòng lặp.
</must_haves>
