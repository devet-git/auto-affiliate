---
phase: 03
plan: 02
wave: 2
depends_on: [03-01]
files_modified:
  - server/app/domains/content_sourcing/services/facebook_seeding.py
  - server/app/domains/sys_worker/tasks/seeding_tasks.py
autonomous: false
requirements: [SEED-02]
status: pending
---

# Plan 03-02: Facebook Auto-Seeding & Commenting Worker

## Objective
Xây dựng logic tự động kết nối với Appium để điều khiển điện thoại thật và thực thi thao tác spam Comment Link Affiliate vào các Group mục tiêu. Thỏa mãn SEED-02.

## Tasks

<task id="03-02-01" autonomous="true">
  <action>
    Implement Appium Python Client script for Facebook Commenting.
    - Create `server/app/domains/content_sourcing/services/facebook_seeding.py`.
    - Setup `AppiumOptions` configured specifically with `UiAutomator2Options` connecting to `127.0.0.1:4723`.
    - Provide a method `comment_on_post(udid: str, post_id: str, text: str)` that launches Facebook app intent, finds comment box, and types text.
  </action>
  <read_first>
    - .planning/phases/03-content-sourcing-social-seeding/03-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `server/app/domains/content_sourcing/services/facebook_seeding.py` contains `UiAutomator2Options`.
    - Contains `webdriver.Remote('http://127.0.0.1:4723'` logic.
  </acceptance_criteria>
</task>

<task id="03-02-02" autonomous="true">
  <action>
    Create Celery tasks to trigger Seeding.
    - Create or update `server/app/domains/sys_worker/tasks/seeding_tasks.py`.
    - Map Celery task `exec_fb_comment` that calls `facebook_seeding.comment_on_post`. Ensure queue separation logic (e.g. `queue='appium_phone'`) to avoid blocking fast API web workers.
  </action>
  <read_first>
    - server/app/domains/content_sourcing/services/facebook_seeding.py
  </read_first>
  <acceptance_criteria>
    - `server/app/domains/sys_worker/tasks/seeding_tasks.py` contains `@celery_app.task(queue='appium_phone')`.
  </acceptance_criteria>
</task>

## Verification

<must_haves>
- [ ] Appium connection logic uses `UiAutomator2Options` specifically (Appium v2 standard).
- [ ] Celery task is mapped properly to decouple phone execution from main backend API.
</must_haves>
