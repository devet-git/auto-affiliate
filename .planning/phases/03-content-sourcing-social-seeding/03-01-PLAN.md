---
phase: 03
plan: 01
wave: 1
depends_on: []
files_modified:
  - server/app/domains/content_sourcing/router.py
  - server/app/domains/content_sourcing/services/scraper.py
  - server/app/domains/content_sourcing/services/ffmpeg_dedupe.py
  - tests/test_scraper.py
  - tests/test_ffmpeg.py
autonomous: false
requirements: [SEED-01]
status: pending
---

# Plan 03-01: Hot Video Crawler & Downloader

## Objective
Xây dựng module Crawler chạy ngầm giúp cào/tìm kiếm link ảnh/video từ nguồn (TikTok/Douyin api hoặc công cụ yt-dlp) và thiết lập tầng FFmpeg Deduplication (Wipe metadata / Fip video) cho Phase 3. Thỏa mãn SEED-01.

## Tasks

<task id="03-01-01" autonomous="true">
  <action>
    Create the `content_sourcing` domain and implement ffmpeg deduplication logic.
    - Create `server/app/domains/content_sourcing/services/ffmpeg_dedupe.py`.
    - Function `apply_light_dedupe(input_path: str, output_path: str)`: use `ffmpeg-python` to map metadata to -1 (wipe ID3/exif tags).
    - Function `apply_deep_dedupe(input_path: str, output_path: str)`: apply `hflip` and speed change `atempo=1.05`/`setpts=0.95*PTS`.
  </action>
  <read_first>
    - .planning/phases/03-content-sourcing-social-seeding/03-CONTEXT.md
    - .planning/phases/03-content-sourcing-social-seeding/03-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `server/app/domains/content_sourcing/services/ffmpeg_dedupe.py` contains `apply_light_dedupe` definition.
    - File contains `ffmpeg.input` and `map_metadata='-1'`.
  </acceptance_criteria>
</task>

<task id="03-01-02" autonomous="true">
  <action>
    Implement Video Scraper logic.
    - Create `server/app/domains/content_sourcing/services/scraper.py`.
    - Create a class `BaseVideoSource` and a stub `TikTokSource(BaseVideoSource)` that returns mock `.mp4` URLs or uses `yt-dlp` to download to internal storage `/tmp/videos/`.
  </action>
  <read_first>
    - server/app/domains/content_sourcing/services/ffmpeg_dedupe.py
  </read_first>
  <acceptance_criteria>
    - `server/app/domains/content_sourcing/services/scraper.py` contains `BaseVideoSource` class.
    - Contains `yt_dlp` import or mock data representation for SEED-01.
  </acceptance_criteria>
</task>

## Verification

<must_haves>
- [ ] FFmpeg functions to deduplicate MP4 files correctly map metadata -1 or flip video.
- [ ] The Scraper module has an interface designed for multi-source expansion.
</must_haves>
