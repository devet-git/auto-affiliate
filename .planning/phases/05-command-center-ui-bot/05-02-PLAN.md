---
wave: 2
depends_on: ["05-01-PLAN.md"]
files_modified:
  - web/src/pages/ApprovalQueue.tsx
  - server/app/domains/approval/router.py
autonomous: false
requirements:
  - UI-01
  - UI-02
---

# Plan 05-02: Màn hình Approval Queue & Manage Grid/Table View

<objective>
Tạo giao diện kiểm duyệt nội dung trung tâm (Approval Queue) cho phép load các Video/Post pending từ database. Hỗ trợ 2 chế độ View (Grid và Table) và Dialog Edit Inline đối với Caption trước khi gửi lệnh Approve.
</objective>

## Tasks

<task>
<read_first>
- web/src/pages/Dashboard.tsx (nếu có, hoặc App.tsx)
- server/app/domains/approval/router.py (nơi sẽ viết API trả list pending queue)
</read_first>
<action>
1. Backend: Bổ sung endpoint `GET /api/approval/queue` vào file router.py trả về mảng các `PendingPostSchema` (id, title, caption, mp4_url, status).
2. Backend: Bổ sung endpoint `PUT /api/approval/{id}` nhận Body `{ caption: str, status: 'approved' | 'rejected' }`.
3. Frontend: Thư mục `web/src/pages/ApprovalQueue.tsx` (route `/dashboard/approval`) gọi Axios fetch API List.
</action>
<acceptance_criteria>
- File `server/app/domains/approval/router.py` định nghĩa endpoint `/queue` trả về mảng list dictionary JSON.
- `ApprovalQueue.tsx` đã thiết lập state dùng hook `useState` lưu mảng `data` và dùng `useEffect` (hoặc `react-query`) fetch `/api/approval/queue`.
</acceptance_criteria>
</task>

<task>
<read_first>
- web/src/pages/ApprovalQueue.tsx
</read_first>
<action>
1. Tại thư mục `web/src/components/`, tạo thêm Component `CardView` và `TableView`. Sử dụng `npx shadcn@latest add table card dialog textarea`.
2. Truyền state `viewMode` ("grid" hay "table") ở `ApprovalQueue.tsx`. Render điều kiện component `CardView` (cho "grid") hoặc list `TableView`.
3. Bọc một Dialog (`shadcn/dialog`) vào mỗi Item có hình bút chì Edit. Dialog hiển thị 1 `Textarea` cho phép nhập Caption mới. Bấm "Save & Approve", gọi tới `PUT /api/approval/{id}` với chuỗi caption.
4. Update UI Optimistic: Báo Toast success và filter gỡ bỏ Item đó khỏi màn hình ngay lập tức mà không cần F5.
</action>
<acceptance_criteria>
- Component xuất các nút bấm gọi method App/Reject chứa hàm Gọi API `axios.put(..., {status: 'approved'})`.
- State `viewMode` cho phép ấn toggle Grid vs Table layout.
- Dialog mở và thay đổi Caption thực tế trên State.
</acceptance_criteria>
</task>

<task>
<read_first>
- web/src/pages/Campaigns.tsx
- server/app/domains/campaigns/router.py
</read_first>
<action>
1. Tạo UI `web/src/pages/Campaigns.tsx` để hiển thị Table Dashboard quản lý các Campaign (Shopee crawler jobs).
2. Tích hợp `shadcn/form` và `Dialog` để tạo Form "Add New Campaign". Form chứa các field: Name, URL, Tags, Active Status.
3. Submit Form gọi API `POST /api/campaigns` truyền payload JSON từ thẻ input vào backend router. Xoá/Edit tương tự bằng modal.
</action>
<acceptance_criteria>
- File `web/src/pages/Campaigns.tsx` chứa `fetch('/api/campaigns')` và Data Form Create/Update.
- Frontend App.tsx có route `/dashboard/campaigns` load layout này.
</acceptance_criteria>
</task>

## Verification
- Chạy npm dev, mở trang Approval Queue.
- Chuyển Grid/Table và test Inline Edit "Edit" text thành "New Text Affiliate", bấm Save & Approve -> Xem log network gửi method PUT và JSON body về backend, sau đó UI tự khép.

## Must Haves
- Toggle icon grid vs table hoạt động.
- Sửa trực tiếp caption trên dialog (inline edit).
