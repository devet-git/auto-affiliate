# === ADMIN AUTH (Personal Use Only) ===
# Generate bcrypt hash with: python -c "from passlib.context import CryptContext; print(CryptContext(schemes=['bcrypt']).hash('yourpassword'))"
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH="$2b$12$EixZaYVK1fsbw1ZfbX3OXePaWxn96p36WQoeG6Lrj3GGx1q0Kx.e"

# === JWT ===
SECRET_KEY=changethis-to-a-long-random-secret-key-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# === DATABASE ===
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/auto_affiliate

# === REDIS & CELERY ===
REDIS_URL=redis://localhost:6379/0
